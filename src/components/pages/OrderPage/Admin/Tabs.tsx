import styled from "styled-components";

const Tabs = () => {
  return <TabsStyled>Tabs</TabsStyled>;
};

export default Tabs;

const TabsStyled = styled.div`
  height: 44px;
  border: 1px solid blue;
`;